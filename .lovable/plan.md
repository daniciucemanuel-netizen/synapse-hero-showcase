

Remove the "AI-Powered Growth" pill/kicker badge from the Hero section.

### Change
**`src/components/HeroSection.tsx`**: Delete the `motion.div` element containing the pill (the one with "AI-Powered Growth" text and the pulsing dot), around lines 24-33.

